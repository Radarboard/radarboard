/**
 * __WIDGET_NAME__ — Data types
 *
 * Define widget-specific normalized source data here. Per-instance widget
 * config now defaults to WidgetTemplateConfig in index.ts.
 */

/** Data shape returned by the hook. */
export type __WIDGET_PASCAL__Data = {};
