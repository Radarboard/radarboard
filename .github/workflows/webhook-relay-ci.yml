name: Webhook Relay CI

on:
  push:
    branches: [main]
    paths:
      - ".github/actions/**"
      - "apps/webhook-relay/**"
      - "packages/integrations/**"
      - "packages/types/**"
      - "pnpm-lock.yaml"
  pull_request:
    paths:
      - ".github/actions/**"
      - "apps/webhook-relay/**"
      - "packages/integrations/**"
      - "packages/types/**"
      - "pnpm-lock.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      statuses: write

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Node, pnpm, and dependencies
        uses: ./.github/actions/setup-pnpm-install
        with:
          node-version-file: .nvmrc
          turbo-cache: "true"

      - name: Typecheck
        run: pnpm exec turbo run typecheck --filter=@radarboard/apphook-relay

      - name: Lint
        run: pnpm exec biome check apps/webhook-relay/

      - name: notify vercel
        uses: vercel/repository-dispatch/actions/status@v1
        with:
          name: "Vercel - radarboard-webhook-relay: lint"

      - name: Test
        run: pnpm exec turbo run test --filter=@radarboard/apphook-relay

      - name: Build
        run: pnpm exec turbo run build --filter=@radarboard/apphook-relay
