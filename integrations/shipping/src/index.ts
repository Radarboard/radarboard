/**
 * Shipping — Virtual integration (no descriptor)
 *
 * Aggregates merged PRs (GitHub), completed issues (Linear),
 * and production deploys (Vercel) into a unified shipping log.
 */

export { shippingDataSources } from "./api/data-sources";
