/** Canonical modal sizes for consistent overlays across the system. */
export type ModalSize = "sm" | "md" | "lg";

/** Backward-compatible alias for dialog content size. */
export type ModalContentSize = ModalSize;
