{
  "name": "@radarboard/embedding-service",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "exports": {
    ".": "./src/index.ts",
    "./similarity": "./src/similarity.ts"
  },
  "dependencies": {
    "@radarboard/llm": "workspace:*",
    "@radarboard/types": "workspace:*"
  },
  "devDependencies": {
    "@radarboard/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
