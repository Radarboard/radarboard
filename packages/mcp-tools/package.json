{
  "name": "@radarboard/mcp-tools",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./auth": "./src/auth.ts"
  },
  "dependencies": {
    "@radarboard/plugin-sdk": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@radarboard/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
