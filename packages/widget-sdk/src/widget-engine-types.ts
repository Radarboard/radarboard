/**
 * Widget type definitions — re-exported from @radarboard/widget-sdk for backward compatibility.
 *
 * The canonical definitions now live in @radarboard/widget-sdk/widget-types.
 * New code should import directly from `@radarboard/widget-sdk/widget-types`.
 */
export type {
  GridSlot,
  WidgetAuth,
  WidgetAuthField,
  WidgetDescriptor,
  WidgetDisplayContext,
  WidgetExpandAction,
  WidgetModalSize,
  WidgetOAuthConfig,
  WidgetPollingConfig,
  WidgetRenderProps,
  WidgetTemplateVisualEditorBinding,
  WidgetVisualEditorBinding,
} from "@radarboard/widget-sdk/widget-types";
