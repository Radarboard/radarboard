import { createPageMetadata, DASHBOARD_DESCRIPTION } from "@/app/metadata";

export const metadata = createPageMetadata({
  description: DASHBOARD_DESCRIPTION,
});

export default function DashboardPage() {
  return null;
}
